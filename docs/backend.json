
{
  "entities": {
    "Turf": {
      "title": "Turf",
      "description": "A sports turf venue listing with facilities and coaching services",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "location": { "type": "string" },
        "area": { "type": "string" },
        "sportTypes": { "type": "array", "items": { "type": "string", "enum": ["Cricket", "Football", "Pickleball"] } },
        "courtTypes": { "type": "array", "items": { "type": "string" } },
        "pricePerHour": { "type": "number", "description": "Base or starting price per hour" },
        "courtPricing": { 
          "type": "object", 
          "additionalProperties": { "type": "number" },
          "description": "Mapping of court type names to their specific hourly rates"
        },
        "rating": { "type": "number" },
        "reviewCount": { "type": "number" },
        "images": { "type": "array", "items": { "type": "string" } },
        "description": { "type": "string" },
        "amenities": { "type": "array", "items": { "type": "string" } },
        "coachingServices": { "type": "array", "items": { "type": "string" } },
        "openingHours": { "type": "string" },
        "contactNumber": { "type": "string" },
        "whatsappNumber": { "type": "string" },
        "mapUrl": { "type": "string" },
        "isPopular": { "type": "boolean" },
        "views": { "type": "number" },
        "whatsappClicks": { "type": "number" },
        "updatedAt": { "type": "object" }
      },
      "required": ["name", "area", "pricePerHour"]
    },
    "Analytics": {
      "title": "Analytics",
      "description": "Global application usage statistics",
      "type": "object",
      "properties": {
        "totalUsers": { "type": "number" },
        "totalViews": { "type": "number" },
        "totalWhatsAppClicks": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/turfs/{turfId}": {
      "schema": "Turf",
      "description": "Collection of sports turfs"
    },
    "/analytics/stats": {
      "schema": "Analytics",
      "description": "Global usage statistics"
    }
  }
}
