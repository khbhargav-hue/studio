
/**
 * Hardened Data Node
 * All mock data has been redacted to ensure the circuit only reads from production Firestore collections.
 */
import { Turf } from './types';

export const MOCK_TURFS: Turf[] = [];
